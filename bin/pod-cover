#!/usr/bin/perl
use strict;
use warnings;
close DATA;

use Getopt::Long qw(:config gnu_getopt);
GetOptions(
  'parents!'  => \(my $parents = 1),
  'roles!'    => \(my $roles = 1),
) or die "Invalid options!\n";

my @files = @ARGV;


__END__
